### 配置初始化
说明：初始化配置、全局变量、全局共享资源，提供统一的初始化入口（程序启动前调用）
```shell
 1. mysql
 2. redis
 3. log 
 4. monitor
...
```
### 名词解释
Initialize（动词）是执行“初始化”动作的过程，用于给变量、对象或系统赋初值；Initialization（名词）是指初始化这一行为、过程或状态本身。简单来说，initialize 是动作，initialization 是该动作的名词称呼，两者均旨在确保程序运行前参数已配置。 